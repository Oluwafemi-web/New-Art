import blockContent from './blockContent'

import carousel from './carousel'
import history from './history'
import steps from './steps'

export const schemaTypes = [carousel, steps, history, blockContent]
